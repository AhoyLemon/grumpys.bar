'use strict';

var _ref;

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

$('.hamburger').click(function () {
  $('header.mobile').toggleClass('active');
});

var cal = $('.calendar');
var singleSlideWidth = 820;

cal.on('init', function () {
  matchCalendarHeight();
});

cal.slick({
  infinite: true,
  slidesToShow: 3,
  slidesToScroll: 1,
  adaptiveHeight: false,
  prevArrow: false,
  nextArrow: '<button type=button" class="slick-next"></button>',
  responsive: [(_ref = {
    breakpoint: 1150,
    settings: {
      slidesToShow: 2
    }
  }, _defineProperty(_ref, 'breakpoint', singleSlideWidth), _defineProperty(_ref, 'settings', {
    slidesToShow: 1,
    adaptiveHeight: true
  }), _ref)]
});

$('.image-carousel').slick({
  infinite: true,
  slidesToShow: 1,
  arrows: false,
  dots: true
});

function matchCalendarHeight() {

  if ($(window).width > singleSlideWidth) {
    var maxHeight = void 0;
    $('.calendar .inner').each(function () {
      maxHeight = maxHeight > $(this).height() ? maxHeight : $(this).height();
    });

    $('.calendar .inner').each(function () {
      $(this).height(maxHeight);
    });
  }
}

$("a[scroll-to]").click(function () {

  var o = 0;
  if (window.innerWidth < 601) {
    o = 120;
  }

  var t = $(this).attr('scroll-to');
  $('header.mobile').removeClass('active');
  $([document.documentElement, document.body]).animate({
    scrollTop: $('a[name=' + t + ']').offset().top - o
  }, 350);
});
//# sourceMappingURL=grumpys.min.js.map